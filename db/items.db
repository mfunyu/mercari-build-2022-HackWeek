CREATE TABLE IF NOT EXISTS items (
	id TEXT PRIMARY KEY,
	name TEXT,
	category_id TEXT,
	image TEXT,
	price INT,
	is_auction INT,
	on_sale INT
);

CREATE TABLE IF NOT EXISTS category (
	id TEXT PRIMARY KEY,
	name TEXT UNIQUE
);