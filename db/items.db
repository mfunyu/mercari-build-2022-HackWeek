CREATE TABLE IF NOT EXISTS items (
	id TEXT PRIMARY KEY,
	name TEXT,
	category_id TEXT,
	image TEXT,
	is_auction INT
);

CREATE TABLE IF NOT EXISTS category (
	id TEXT PRIMARY KEY,
	name TEXT UNIQUE
);